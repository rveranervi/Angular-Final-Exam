export interface CountryConfig {
    name: string,
    uri: string,
    country: string,
    countryLabel: string
}
export interface LanguageConfig {
    name: string,
    label: string,
    abrev: string
}
export interface CurrencyConfig {
    name: string,
    label: string,
    symbol: string
}